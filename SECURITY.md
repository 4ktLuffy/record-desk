# Security

This application binds to loopback and has no authentication or tenant separation. Do not expose it through a public tunnel or reverse proxy. CSV files are untrusted input. Document processing can transmit extracted text to the configured provider when explicitly requested in the interface.

Do not post credentials or private documents in issues. Report vulnerabilities privately through GitHub private vulnerability reporting if enabled; otherwise request a private reporting channel without publishing exploit details.
